The config layer is the glue giving the service layer the instances of the
pluged in layers. 

Basicly all kinds of factories or configurations as they are called in spring
reside here and therefore allows dependency injection. The factories are
manged through spring.
